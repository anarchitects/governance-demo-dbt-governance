version: 2

models:
  # Intentional demo smell: this model omits owner/domain/criticality metadata
  # so governance checks can surface explainable findings in reports.
  - name: unowned_customer_summary
    description: Segment-level customer summary used to demonstrate governance smells.
    config:
      tags: ["intentionally_problematic"]
